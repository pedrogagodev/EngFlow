{
  "name": "engflow",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "check": "bun test && bunx tsc --noEmit -p tsconfig.json",
    "daemon": "bun run --cwd packages/daemon start",
    "daemon:dev": "bun run --cwd packages/daemon dev",
    "stack": "bun ./scripts/engflow-stack.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
